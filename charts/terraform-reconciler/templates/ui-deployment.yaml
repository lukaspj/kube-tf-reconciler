{{- if .Values.ui.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/version: {{ .Values.image.tag }}
    app.kubernetes.io/name: {{ .Values.appName }}-ui
    app.kubernetes.io/component: dashboard
    app.kubernetes.io/part-of: {{ .Values.appName }}
  name: {{ .Values.appName }}-ui
spec:
  replicas: {{ .Values.ui.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.appName }}-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/version: {{ .Values.image.tag }}
        app.kubernetes.io/name: {{ .Values.appName }}-ui
        app.kubernetes.io/component: dashboard
        app.kubernetes.io/part-of: {{ .Values.appName }}
        app: {{ .Values.appName }}-ui
    spec:
      serviceAccountName: {{ .Values.appName }}-ui
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Values.appName }}-ui
        image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        command: ["krec"]
        args:
          - "ui"
          - "--in-cluster"
          - "--port"
          - "8080"
          - "--namespace"
          - {{ .Values.ui.namespace | quote }}
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /api/config
            port: http
        livenessProbe:
          httpGet:
            path: /api/config
            port: http
        {{- if .Values.ui.auth.enabled }}
        env:
        - name: KREC_UI_OAUTH_ISSUER
          value: {{ required "ui.auth.issuer is required" .Values.ui.auth.issuer | quote }}
        - name: KREC_UI_OAUTH_CLIENT_ID
          value: {{ required "ui.auth.clientID is required" .Values.ui.auth.clientID | quote }}
        - name: KREC_UI_OAUTH_CLIENT_SECRET
          value: {{ required "ui.auth.clientSecret is required" .Values.ui.auth.clientSecret | quote }}
        - name: KREC_UI_OAUTH_REDIRECT_URL
          value: {{ .Values.ui.auth.redirectURL | quote }}
        - name: KREC_UI_OAUTH_SCOPES
          value: {{ .Values.ui.auth.scopes | quote }}
        - name: KREC_UI_SESSION_SECRET
          value: {{ .Values.ui.auth.sessionSecret | quote }}
        {{- end }}
{{- end }}
